{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>NumeroSalle</th>
                <td>{{ salle.NumeroSalle }}</td>
            </tr>
            <tr>
                <th>ProfSurveillant</th>
                <td>{{ salle.ProfSurveillant }}</td>
            </tr>
            <tr>
                <th>DimensionSalle</th>
                <td>{{ salle.DimensionSalle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'id': salle.id}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
